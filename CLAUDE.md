# Claude Code - Proje Talimatları

## Git Push Kuralı
Tüm değişiklikleri direkt `main` branch'e push et.
`claude/` branch kullanma — Render `main`'den otomatik deploy eder.

```
git add <dosya>
git commit -m "açıklama"
git push origin main
```

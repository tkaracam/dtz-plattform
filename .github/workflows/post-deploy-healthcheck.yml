name: Post-Deploy Healthcheck

on:
  push:
    branches:
      - main
  schedule:
    - cron: "15 6 * * *"
  workflow_dispatch:
    inputs:
      target_urls:
        description: "Birden fazla URL (boşlukla ayırın)"
        required: false
        default: ""
      base_url:
        description: "Tek URL (geriye dönük; target_urls boşsa kullanılır)"
        required: false
        default: "https://dtz-lid.com"
      warmup_seconds:
        description: "Deploy sonrası bekleme (saniye)"
        required: false
        default: "180"
      max_attempts:
        description: "Maksimum deneme sayısı"
        required: false
        default: "8"
      retry_delay_seconds:
        description: "Hata sonrası bekleme (saniye)"
        required: false
        default: "30"

permissions:
  contents: read

concurrency:
  group: post-deploy-healthcheck-${{ github.ref }}
  cancel-in-progress: true

jobs:
  healthcheck:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    env:
      TARGET_URLS: ${{ github.event_name == 'workflow_dispatch' && (github.event.inputs.target_urls || github.event.inputs.base_url) || 'https://dtz-lid.com https://www.dtz-lid.com' }}
      WARMUP_SECONDS: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.warmup_seconds || '180' }}
      MAX_ATTEMPTS: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.max_attempts || '8' }}
      RETRY_DELAY_SECONDS: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.retry_delay_seconds || '30' }}
      HC_STUDENT_USERNAME: ${{ secrets.HC_STUDENT_USERNAME }}
      HC_STUDENT_PASSWORD: ${{ secrets.HC_STUDENT_PASSWORD }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate numeric inputs
        run: |
          set -euo pipefail
          case "${WARMUP_SECONDS}" in ''|*[!0-9]*) echo "WARMUP_SECONDS sayi olmali"; exit 1;; esac
          case "${MAX_ATTEMPTS}" in ''|*[!0-9]*) echo "MAX_ATTEMPTS sayi olmali"; exit 1;; esac
          case "${RETRY_DELAY_SECONDS}" in ''|*[!0-9]*) echo "RETRY_DELAY_SECONDS sayi olmali"; exit 1;; esac
          [ "${MAX_ATTEMPTS}" -ge 1 ] || { echo "MAX_ATTEMPTS en az 1 olmali"; exit 1; }

      - name: Prepare healthcheck script
        run: chmod +x tools/healthcheck.sh

      - name: Warmup (wait for Render deploy)
        run: |
          echo "Waiting ${WARMUP_SECONDS}s for deployment propagation..."
          sleep "${WARMUP_SECONDS}"

      - name: Run healthcheck with retry
        run: |
          set -euo pipefail
          LOG_FILE="${RUNNER_TEMP}/healthcheck.log"
          : > "${LOG_FILE}"
          START_TS="$(date +%s)"
          overall_failed=0

          normalized_targets="$(printf '%s' "${TARGET_URLS}" | tr ',\n\r\t' '    ' | xargs)"
          if [ -z "${normalized_targets}" ]; then
            normalized_targets="https://dtz-lid.com https://www.dtz-lid.com"
          fi

          # dedupe targets while preserving order
          TARGETS=""
          for candidate in ${normalized_targets}; do
            case " ${TARGETS} " in
              *" ${candidate} "*) ;;
              *) TARGETS="${TARGETS} ${candidate}" ;;
            esac
          done
          TARGETS="$(printf '%s' "${TARGETS}" | xargs)"
          [ -n "${TARGETS}" ] || { echo "Hedef URL listesi bos olamaz"; exit 1; }

          passed_targets=""
          failed_targets=""

          {
            echo "## Post-Deploy Healthcheck"
            echo ""
            echo "- Targets: \`${TARGETS}\`"
            echo "- Trigger: \`${{ github.event_name }}\`"
            echo "- Commit: \`${{ github.sha }}\`"
            echo ""
          } >> "${GITHUB_STEP_SUMMARY}"

          for BASE_URL in ${TARGETS}; do
            attempt=1
            passed=0
            while [ "$attempt" -le "$MAX_ATTEMPTS" ]; do
              echo "Attempt ${attempt}/${MAX_ATTEMPTS} -> ${BASE_URL}"
              if ./tools/healthcheck.sh "${BASE_URL}" 2>&1 | tee -a "${LOG_FILE}"; then
                passed=1
                break
              fi
              if [ "$attempt" -lt "$MAX_ATTEMPTS" ]; then
                echo "Healthcheck failed for ${BASE_URL}. Retrying in ${RETRY_DELAY_SECONDS}s..."
                sleep "${RETRY_DELAY_SECONDS}"
              fi
              attempt=$((attempt + 1))
            done

            if [ "$passed" -eq 1 ]; then
              passed_targets="${passed_targets} ${BASE_URL}"
              {
                echo "- ✅ \`${BASE_URL}\` passed (\`${attempt}/${MAX_ATTEMPTS}\`)"
              } >> "${GITHUB_STEP_SUMMARY}"
            else
              overall_failed=1
              failed_targets="${failed_targets} ${BASE_URL}"
              {
                echo "- ❌ \`${BASE_URL}\` failed after \`${MAX_ATTEMPTS}\` attempts"
              } >> "${GITHUB_STEP_SUMMARY}"
            fi
          done

          END_TS="$(date +%s)"
          ELAPSED="$((END_TS - START_TS))"

          {
            echo ""
            echo "### Summary"
            echo "- Dauer: \`${ELAPSED}s\`"
            echo "- Erfolgreich: \`$(printf '%s' "${passed_targets}" | xargs || true)\`"
            if [ -n "$(printf '%s' "${failed_targets}" | xargs || true)" ]; then
              echo "- Fehlgeschlagen: \`$(printf '%s' "${failed_targets}" | xargs)\`"
            else
              echo "- Fehlgeschlagen: \`keine\`"
            fi
          } >> "${GITHUB_STEP_SUMMARY}"

          if [ "$overall_failed" -ne 0 ]; then
            {
              echo ""
              echo "### Last log lines"
              echo '```'
              tail -n 120 "${LOG_FILE}" || true
              echo '```'
            } >> "${GITHUB_STEP_SUMMARY}"
            exit 1
          fi

      - name: Upload healthcheck log artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: healthcheck-log-${{ github.run_id }}
          path: ${{ runner.temp }}/healthcheck.log
          if-no-files-found: ignore
